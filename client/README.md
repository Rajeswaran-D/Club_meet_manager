# ClubMeet Client

This is the frontend application for ClubMeet, built with React and Vite.

## Setup
```bash
npm install
npm run dev
```

## Environment Variables
Create a `.env` file based on `.env.example`:
```
VITE_API_URL="http://localhost:5000/api"
```
